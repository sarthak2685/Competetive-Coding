#include <iostream>
#include <string>

using namespace std;

int main() {
    int t;
    cin >> t;

    while (t--) {
        char c;
        cin >> c;

        string s = "codeforces";

        if (s.find(c) != string::npos) {
            cout << "YES\n";
        } else {
            cout << "NO\n";
        }
    }

    return 0;
}
