#include<bits/stdc++.h>
using namespace std;
int main(){
  int n=3;
  int num[4];
  for(int i=0;i<n;i++){
      cin>>num[i];
  }
  sort(num,num+3);
   int a,b,c;
   a=abs(num[0]-num[1]);
   b=abs(num[1]-num[2]);
   c=a+b;
   cout<<c;
  return 0;
}
