 #include<bits/stdc++.h>
using namespace std;
int main(){
    string s,c,g;
    cin>>s>>c>>g;
    s=s+c;
    sort(s.begin(),s.end());
    sort(g.begin(),g.end()); 
    if(s==g) cout<<"YES";
    else cout<<"NO";
    
    return 0;
    
    
    
}
